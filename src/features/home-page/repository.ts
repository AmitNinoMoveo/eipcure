import mockHomePageData from "../../utils/constants/mockData/mockHomePageData";


export const getHomePageDataRepo = () => {
    // const response = await api.get('');
    // return new Promise((resolve, reject): Promise<HomePage | undefined>=>{
    //         resolve(mockHomePageData as HomePage);
    // });
    return mockHomePageData;
}